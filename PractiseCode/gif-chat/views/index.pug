doctype
html
    head
        meta(charset='utf-8')
        title GIT 채팅방
    body
        div F12 눌러 console 탭과 network 탭을 확인하세요.
        script(src='/socket.io/socket.io.js')
        script.
            var socket = io.connect('http"//localhost:8005', {
                path : '/socket.io',
            });
            socket.on('news', function (hdata) {
                console.log(data);
                socket.emit('reply', 'Hello Node.JS');
            });